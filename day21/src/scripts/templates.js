// let paragraph = `The apple is red.
// The bannana is yellow.
// The plum is purple.`;

let time = 300; // in minutes in 24 hour format
let message = `Lets meet in ${time/60} hours.`;

console.log(message);

/*
    Template Literals: 
    - template literals are string literals allowing embedded expressions. 
    - you can use multi-line strings and string interpolation features with them.
*/